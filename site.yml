- hosts: localhost
  tasks:
    - name: install packages
      become: true
      become_user: root
      apt:
        state: present
        name:
         - vim
         - firefox
         - docker
         - wireshark
         - steam
         - docker.io
    - name: uninstall packages
      become: true
      become_user: root
      apt:
        state: absent
        name:
         - nano
    - name: autoupdate
      lineinfile:
        path: /etc/crontab
        line: 0,15,30,45 * * * * root apt update ; apt upgrade -y ; apt autoremove -y
        insertbefore: BOF
    - name: dockerlatest
      apt:
        name: docker.io
        state: latest
    - name: dockerrunning
      service:
        name: docker.io
        state: started
